upstream varnish {
  {% for host in groups['varnish'] %}
  server {{host}}:7073;
  {% endfor %}
}

server {
  listen 80;
  server_name {{varnish_host}} {{forum_varnish_host}};

  location / {
    expires max;
    proxy_pass http://varnish;
  }
}
